import { createAction } from '@ngrx/store';

export const increment = createAction('[NgRxComponent] Increment');
export const decrement = createAction('[NgRxComponent] Decrement');
export const reset = createAction('[NgRxComponent] Reset');